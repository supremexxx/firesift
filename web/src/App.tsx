function App() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-6 px-6 text-center">
      <span className="text-sm font-medium tracking-widest text-muted uppercase">
        FireSift
      </span>
      <h1 className="max-w-2xl text-5xl font-semibold tracking-tight text-balance sm:text-6xl">
        The interface is being rebuilt.
      </h1>
      <p className="max-w-md text-lg text-muted text-balance">
        This is a placeholder confirming the React + Tailwind build
        pipeline is wired up end to end.
      </p>
      <span className="mt-4 inline-block h-1 w-12 rounded-full bg-accent" />
    </main>
  )
}

export default App
